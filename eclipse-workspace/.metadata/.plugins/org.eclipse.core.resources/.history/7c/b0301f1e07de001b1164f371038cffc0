package p;

public class B extends A {
	void set() {
		pub = 1;	// public : 
		pro = 2;	// protected : 
		def = 3;	// default : 같은 패키지 일때 접근, 다른 패키지에서는 접근 불가
		pri = 4;	// private : 다른 클래스에서 접근 차단
	}
}
